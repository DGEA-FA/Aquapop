% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortalite_prepare_extended.R
\name{mortalite_prepare_extended}
\alias{mortalite_prepare_extended}
\title{Étendre artificiellement les données de fréquence d'âge avec des zéros}
\usage{
mortalite_prepare_extended(df_corrigee, age_max)
}
\arguments{
\item{df_corrigee}{Un \code{data.frame} contenant les colonnes \code{age} (âge) et
\code{number} (fréquence), généralement produit par \code{mortalite_prepare_corr()}.}

\item{age_max}{Un entier indiquant l'âge maximal observé, généralement issu
de \code{mortalite_get_age_max()}.}
}
\value{
Une liste avec les éléments suivants :
\describe{
\item{success}{Un booléen indiquant si la préparation a réussi.}
\item{message}{Un message informatif si la préparation est impossible, sinon \code{NULL}.}
\item{data}{Un \code{data.frame} combinant les âges observés avec les âges fictifs ajoutés, ou \code{NULL} si la préparation est impossible.}
}
}
\description{
Cette fonction ajoute des classes d'âge fictives avec un nombre de captures nul
(\code{number = 0}) au-delà de l'âge maximal observé, jusqu'à trois fois cet âge.
Cette étape permet d'améliorer l'ajustement de certains modèles de mortalité.
}
\details{
La fonction retourne toujours une liste structurée contenant :
\itemize{
\item \code{success} : indicateur logique de réussite
\item \code{message} : message informatif si la préparation est impossible
\item \code{data} : tableau étendu des fréquences d'âge, ou \code{NULL} si indisponible
}
}
\examples{
df <- data.frame(age = 2:5, number = c(4, 3, 2, 1))
mortalite_prepare_extended(df, age_max = 5)
}
