% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue_select_best_modele.R
\name{cpue_select_best_modele}
\alias{cpue_select_best_modele}
\title{Sélectionner le meilleur modèle CPUE selon le plus bas AICc}
\usage{
cpue_select_best_modele(tablemodele)
}
\arguments{
\item{tablemodele}{Un \code{data.frame} retourné par
\code{cpue_compare_modele(..., format = "data.frame")}, contenant au minimum
les colonnes \code{methode}, \code{aicc} et \code{ajustement_hnp}.}
}
\value{
Une chaîne de caractères correspondant à la méthode du meilleur modèle sélectionné.
Retourne \code{NA} avec un avertissement si aucun modèle ne peut être sélectionné.
}
\description{
Cette fonction identifie automatiquement le meilleur modèle d’abondance
(CPUE) parmi les modèles Poisson, NB1, NB2, CMP et GP, en se basant sur le
critère d'information corrigé (AICc). La priorité est donnée aux modèles
bien ajustés (ajustement HNP < 10). Si aucun modèle ne satisfait ce critère,
la sélection est effectuée parmi tous les modèles disponibles.
}
\examples{
df <- tibble::tibble(
  methode = c("poisson", "nb1", "nb2"),
  ajustement_hnp = c(5, 12, 9),
  aicc = c(110, 105, 100)
)
cpue_select_best_modele(df)

}
