% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_lac.R
\name{load_lac}
\alias{load_lac}
\title{Charger les données du feuillet "Lac" d'un fichier Excel (version robuste)}
\usage{
load_lac(path, namesheet = "Lac", verbose = TRUE, col_rename = TRUE)
}
\arguments{
\item{path}{Chemin vers le fichier \code{.xlsx} à lire.}

\item{namesheet}{Nom du feuillet contenant les données des lacs (défaut \code{"Lac"}).}

\item{verbose}{Afficher les messages de diagnostic ? (défaut \code{TRUE})}

\item{col_rename}{Appliquer le renommage des colonnes selon la correspondance interne ? (défaut \code{TRUE})}
}
\value{
Un \code{data.frame} structuré avec identifiant unique (\code{ID}) et colonnes normalisées.
}
\description{
Cette fonction importe, nettoie et structure les données associées aux lacs contenues
dans un fichier Excel, avec validations robustes et souples. Les colonnes
essentielles sont obligatoires, les autres sont facultatives et ajoutées si présentes.
}
\details{
Colonnes \strong{obligatoires} : \code{no_lac}, \code{nom_lac}, \code{typ_pech}, \code{annee}

Colonnes \strong{optionnelles} (ajoutées comme \code{NA} si absentes) :
\code{region_admin}, \code{sp_pen}, \code{long_dd.dec}, \code{lat_dd.dec}, \code{terr_faun}, \code{zon_pech},
\code{superficie_ha}, \code{perimetre_km}, \code{prof_max_m}, \code{prof_moy_m}, \code{comments}

La fonction reconnaît également des \strong{variantes courantes} de ces noms grâce à une table
de synonymes nettoyés via \code{make_clean_names()}.
}
\examples{
# Exemple minimal avec un fichier Excel temporaire
df <- data.frame(
  "Nom du lac" = "Lac Test",
  "Numéro LCE" = "00123",
  "Type pêche" = "PE",
  "Année" = "2022"
)
path <- tempfile(fileext = ".xlsx")
write_xlsx(list("Lac" = df), path)
load_lac(path)

}
