% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_specimen.R
\name{load_specimen}
\alias{load_specimen}
\title{Charger les données du feuillet "Specimens" d’un fichier Excel (version robuste)}
\usage{
load_specimen(path, namesheet = "Specimens", verbose = TRUE, col_rename = TRUE)
}
\arguments{
\item{path}{Chemin vers le fichier \code{.xlsx} à lire. Doit contenir un feuillet nommé \code{"Specimens"} formaté selon les conventions AquaPop.}

\item{namesheet}{Nom du feuillet contenant les données des spécimens (défaut \code{"Specimens"}).}

\item{verbose}{Afficher les messages de diagnostic ? (défaut \code{TRUE})}

\item{col_rename}{Appliquer le renommage des colonnes selon la correspondance interne ? (défaut \code{TRUE})}
}
\value{
Un \code{data.frame} structuré avec les colonnes normalisées suivantes :
\itemize{
\item \strong{Obligatoires} : \code{no_lac}, \code{typ_pech}, \code{no_station}, \code{no_specimen}, \code{sp}, \code{ltm}, \code{masse}, \code{age}, \code{sexe}, \code{maturite}, \code{annee}
\item \strong{Optionnelles} (ajoutées comme NA si absentes) : \code{lf}, \code{marquage}, \code{ind_insec}, \code{ind_benth}, \code{ind_planc}, \code{ind_chyme}, \code{ind_vide}, \code{ind_poiss}, \code{poiss1}, \code{poiss2}, \code{comments_specimen}
}
}
\description{
Cette fonction importe, nettoie et structure les données de spécimens de poissons
contenues dans un fichier Excel, avec validations robustes. Les colonnes essentielles
sont obligatoires, les autres sont ajoutées si présentes, sinon remplacées par \code{NA}.
}
\details{
\itemize{
\item Les valeurs manquantes sont remplacées par : \code{"IND"} (\code{sexe}, \code{maturite}), \code{"NMA"} (\code{marquage}).
\item Les années codées au format Excel (5 chiffres) sont converties en année réelle.
\item Les colonnes inutiles sont ignorées.
}
}
\examples{
\dontrun{
library(writexl)
df <- data.frame(
  no_lac      = "001", typ_pech = "PE", no_station = "ST01", no_specimen = "0001",
  sp = "SANA", ltm = "110", masse = "15.3", age = "2", sexe = "F",
  maturite = "O", annee = "2022"
)
path <- tempfile(fileext = ".xlsx")
write_xlsx(list("Specimens" = df), path)
load_specimen(path)
}

}
