% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mortalite_prepare_corr.R
\name{mortalite_prepare_corr}
\alias{mortalite_prepare_corr}
\title{Préparer les données corrigées de fréquence d'âge pour l'estimation de la mortalité}
\usage{
mortalite_prepare_corr(data, age_peak_plus, age_max)
}
\arguments{
\item{data}{Un \code{data.frame} contenant les spécimens d'une seule espèce, avec
une colonne nommée \code{age}. Les valeurs manquantes (\code{NA}) sont automatiquement exclues.}

\item{age_peak_plus}{Un entier indiquant l'âge à partir duquel commence
l'analyse de mortalité.}

\item{age_max}{Un entier indiquant l'âge maximum à considérer pour
l'estimation de la mortalité.}
}
\value{
Une liste avec les éléments suivants :
\describe{
\item{success}{Un booléen indiquant si la préparation a réussi.}
\item{message}{Un message informatif si la préparation est impossible, sinon \code{NULL}.}
\item{data}{Un \code{data.frame} contenant les colonnes \code{age} et \code{number}, ou \code{NULL} si la préparation est impossible.}
}
}
\description{
Cette fonction applique \code{agesurv()} avec \code{type = 1} pour générer les données
individuelles de fréquence d'âge sur la partie descendante de la courbe de capture.
Elle filtre d'abord les spécimens ayant un âge valide, applique l'estimation,
puis complète les classes d'âge manquantes avec des zéros pour produire une
table uniforme.
}
\details{
La fonction retourne toujours une liste structurée contenant :
\itemize{
\item \code{success} : indicateur logique de réussite
\item \code{message} : message informatif si la préparation est impossible
\item \code{data} : tableau de fréquences corrigées, ou \code{NULL} si indisponible
}
}
\examples{
data_exemple <- data.frame(age = c(2, 3, 3, 4, 5, 5, 5, 6, 7, NA))
mortalite_prepare_corr(data = data_exemple, age_peak_plus = 5, age_max = 7)
}
